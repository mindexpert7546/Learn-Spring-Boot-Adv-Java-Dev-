spring:
  application:
    name: OrderService
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/orderdb?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: Krishna7546@#
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8082

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    registry-with-eureka: true
    server-url:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}
